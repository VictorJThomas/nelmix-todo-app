type ProfilesPageProps = {
    isGuest?: boolean;
};

export const ProfilesPage: React.FC<ProfilesPageProps> = ({isGuest}) => {
    return(
        <div>
            Profiles Page
        </div>
    )
}